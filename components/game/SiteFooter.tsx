export function SiteFooter() {
  return (
    <footer className="flex flex-col gap-2 border-t border-slate-200 py-4 text-xs font-semibold text-slate-500 sm:flex-row sm:items-center sm:justify-between">
      <p>ChemPuzzle prototype</p>
      <p>分子判定はゲーム用に単純化した結合グラフです。</p>
    </footer>
  );
}
